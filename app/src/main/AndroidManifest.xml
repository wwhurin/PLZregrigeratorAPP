<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kr.hs.emirim.wwhurin.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".JoinActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".InputActivity" />
        <activity android:name=".Menu1Activity" />
        <activity android:name=".InsertActivity" />
        <activity android:name=".GetItemActivity" />
        <activity android:name=".TIPActivity" />
        <activity android:name=".ReconmmedActivity" />
        <activity android:name=".CodeActivity" />
        <activity android:name=".ShowRecommendActivity" />
        <activity android:name=".ShowTipActivity"></activity>
    </application>

</manifest>